아래는 Flask + Chart.js + pandas + numpy + pymysql를 이용한 학생 성적 관리 CRUD 프로그램 작성

기능

이름, 국어, 영어, 수학 입력
총점, 평균, 학점 계산
MySQL DB 저장
전체조회 / 1명조회 / 수정 / 삭제
학생 이름 기준 평균 Chart.js 그래프 출력
전체 평균과의 차이도 함께 표시
범례(label) 로
학생 평균
전체 평균


create database chartjsschooldb;
use chartjsschooldb;

create table student (
    id int auto_increment primary key,
    name varchar(50) not null,
    kor int not null,
    eng int not null,
    math int not null,
    total int not null,
    avg_score float not null,
    grade varchar(2) not null
);